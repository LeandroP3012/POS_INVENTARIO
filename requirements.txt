# Dependencias del Sistema POS
mysql-connector-python>=8.0.33

# Dependencias opcionales para impresión de códigos de barras
# Instalar solo si necesitas imprimir códigos de barras:
# python-barcode[images]>=0.15.1
# Pillow>=10.0.0
# pywin32>=306

# Dependencias incluidas en Python estándar (no requieren instalación):
# - tkinter (GUI)
# - hashlib (hashing)
# - json (manejo de JSON)
# - datetime (fechas y tiempo)
# - logging (sistema de logs)
# - os (sistema operativo)
# - sys (sistema Python)
# - typing (type hints)
